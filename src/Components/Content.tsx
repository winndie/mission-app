import React from "react"
import '../index.css'

const Content = () => {

  return (
    <div className='border border-primary'>
      <button onClick={()=>{}}>
        Add
      </button>
      <button onClick={()=>{}}>
        Save
      </button>
    </div>
  )
}

export default Content